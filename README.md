# Novaya Borovaya apartment tracker

## Deployment

- run `./deploy.sh`
- after the container is built, you will see `sha digest`
- replace the digest for the main function in serverless.yaml
- `yarn deploy`
